import { styled } from "..";

export const Container = styled('div', {
  display: 'flex',
  gap: 32,
  flexDirection: 'column',
  alignItems: 'flex-start',
  justifyContent: 'center',
  minHeight: '100vh',
})